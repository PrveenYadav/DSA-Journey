#include <iostream>
#include <string>
using namespace std;

//string compression : Leetcode 443 (meduim)
int compress(string ch) {
    int i = 0;
    int ansIndex = 0;
    int n = ch.length();

    while (i<n)
    {
        /* code */
    }
    
}

int main() {
    char ch = 'a';
}